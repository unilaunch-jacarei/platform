{
  "db_name": "PostgreSQL",
  "query": "WITH renewed AS (\n               UPDATE sessoes\n               SET expires_at = LEAST(\n                   NOW() + INTERVAL '8 hours',\n                   created_at + INTERVAL '24 hours'\n               )\n               WHERE id = $1\n                 AND expires_at > NOW()\n                 AND expires_at <= NOW() + INTERVAL '30 minutes'\n               RETURNING usuario_id\n           )\n           SELECT usuario_id FROM renewed\n           UNION ALL\n           SELECT usuario_id FROM sessoes\n           WHERE id = $1 AND expires_at > NOW()\n           LIMIT 1",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "usuario_id",
        "type_info": "Int8"
      }
    ],
    "parameters": {
      "Left": [
        "Text"
      ]
    },
    "nullable": [
      null
    ]
  },
  "hash": "1e1f77cbc731ab48fb8ff7c1e71b8950b6556811fbc5655aa2f63e507393aa8c"
}
